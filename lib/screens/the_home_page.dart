import '../widgets.dart';

class TheHomePage extends StatelessWidget {
  const TheHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const ThePage(title: "Home Page");
  }
}
